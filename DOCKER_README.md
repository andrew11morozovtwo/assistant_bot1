# Docker Deployment для Telegram бота

Этот документ описывает, как развернуть Telegram бота в Docker контейнере на виртуальном сервере.

## 🚀 Быстрый старт

### 1. Подготовка

```bash
# Клонируйте репозиторий (если еще не сделано)
git clone <your-repo-url>
cd it_is_not_chanal

# Создайте файл .env на основе примера
cp env.example .env

# Отредактируйте .env файл и добавьте ваши токены
nano .env
```

### 2. Запуск в Docker

#### Linux/macOS:
```bash
# Сделайте скрипт исполняемым
chmod +x docker-run.sh

# Соберите и запустите контейнер
./docker-run.sh build
./docker-run.sh start
```

#### Windows:
```cmd
# Соберите и запустите контейнер
docker-run.bat build
docker-run.bat start
```

### 3. Проверка работы

```bash
# Посмотреть статус
./docker-run.sh status

# Посмотреть логи
./docker-run.sh logs
```

## 📋 Доступные команды

| Команда | Описание |
|---------|----------|
| `build` | Собрать Docker образ |
| `start` | Запустить контейнер |
| `stop` | Остановить контейнер |
| `restart` | Перезапустить контейнер |
| `logs` | Показать логи в реальном времени |
| `status` | Показать статус и использование ресурсов |
| `cleanup` | Очистить неиспользуемые Docker ресурсы |

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
LOG_FILE_PATH=data/telegram_bot_logs.csv
```

### Docker Compose настройки

- **Память**: Ограничено 512MB, резерв 256MB
- **Перезапуск**: Автоматический при сбоях
- **Healthcheck**: Проверка каждые 30 секунд
- **Volumes**: Логи и данные сохраняются на хосте
- **Команда запуска**: `python -m bot`

### Важные изменения

- **.env файл копируется в образ** из корня проекта при сборке
- **Контейнер может запускаться от root** для упрощения работы с volumes
- **Детальное логирование** всех обращений к OpenAI API для отладки

## 🐳 Развертывание на сервере

### 1. Подготовка сервера

```bash
# Установка Docker (Ubuntu/Debian)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
```

### 2. Загрузка проекта на сервер

```bash
# Через Git
git clone <your-repo-url>
cd it_is_not_chanal

# Или через SCP
scp -r ./it_is_not_chanal user@server:/home/user/
```

### 3. Настройка и запуск

```bash
# Создание .env файла
cp env.example .env
nano .env  # Отредактируйте токены

# Сборка и запуск
./docker-run.sh build
./docker-run.sh start
```

### Альтернативный способ: прямой запуск через docker run

```bash
# Создайте директории
mkdir -p /opt/telegram-bot/data /opt/telegram-bot/logs

# Запустите контейнер от root (для упрощения работы с volumes)
docker run -d --name telegram-bot --restart=always --user root \
  -v /opt/telegram-bot/data:/app/data \
  -v /opt/telegram-bot/logs:/app/logs \
  andrew11morozov/it_is_not_chanal:latest

# Проверка логов
docker logs telegram-bot
```

**Важно:** Образ должен быть собран с вашим `.env` файлом в корне проекта. При сборке `.env` копируется в образ.

### 4. Автозапуск (systemd)

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Bot Docker Container
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/path/to/your/bot
ExecStart=/path/to/your/bot/docker-run.sh start
ExecStop=/path/to/your/bot/docker-run.sh stop
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
```

Активация:
```bash
sudo systemctl enable telegram-bot.service
sudo systemctl start telegram-bot.service
```

## 📊 Мониторинг

### Просмотр логов
```bash
# Все логи
./docker-run.sh logs

# Последние 100 строк
docker-compose logs --tail=100 telegram-bot

# Логи с временными метками
docker-compose logs -t telegram-bot
```

### Статистика ресурсов
```bash
# Текущее использование
./docker-run.sh status

# Детальная статистика
docker stats telegram-bot
```

### Healthcheck
```bash
# Проверка здоровья контейнера
docker inspect telegram-bot | grep -A 10 Health
```

## 🔄 Обновление

```bash
# Остановка
./docker-run.sh stop

# Обновление кода
git pull

# Пересборка и запуск
./docker-run.sh build
./docker-run.sh start
```

## 🛠️ Устранение неполадок

### Контейнер не запускается
```bash
# Проверьте логи
./docker-run.sh logs

# Проверьте статус
./docker-run.sh status

# Перезапустите
./docker-run.sh restart
```

### Проблемы с памятью
```bash
# Очистка неиспользуемых ресурсов
./docker-run.sh cleanup

# Проверка использования диска
docker system df
```

### Проблемы с сетью
```bash
# Проверка сетевых настроек
docker network ls
docker network inspect it_is_not_chanal_bot-network
```

## 📁 Структура файлов

```
it_is_not_chanal/
├── bot.py                 # Основной код бота
├── run_bot.py            # Скрипт запуска
├── requirements.txt       # Python зависимости
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose конфигурация
├── .dockerignore         # Исключения для Docker
├── docker-run.sh         # Скрипт запуска (Linux/macOS)
├── docker-run.bat        # Скрипт запуска (Windows)
├── env.example           # Пример переменных окружения
├── DOCKER_README.md      # Эта документация
├── logs/                 # Логи приложения
└── data/                 # Данные бота
```

## 🔒 Безопасность

- Контейнер может запускаться от root для упрощения работы с volumes (опционально)
- `.env` файл копируется в образ при сборке (убедитесь, что не коммитите реальные токены в git)
- Логи и данные сохраняются в volumes
- Healthcheck для мониторинга состояния

**⚠️ Внимание:** При использовании `.env` в образе убедитесь, что файл `.env` добавлен в `.gitignore` и не попадает в репозиторий с реальными токенами!

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `./docker-run.sh logs`
2. Проверьте статус: `./docker-run.sh status`
3. Перезапустите: `./docker-run.sh restart`
4. Очистите ресурсы: `./docker-run.sh cleanup`